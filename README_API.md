# 🎮 WB Cyber Club - Версия с API и базой данных

Современная платформа для управления киберспортивными турнирами с централизованной базой данных.

## ✨ Что изменилось

### ✅ Было (localStorage):
- Данные хранились локально в браузере
- Каждый пользователь видел свои данные
- Невозможно синхронизировать между устройствами

### 🚀 Стало (PostgreSQL + API):
- **Централизованная база данных** на Neon.tech
- **Данные одинаковые у всех** пользователей
- **Реальное время** - изменения видны сразу
- **Безопасность** - данные на сервере, а не в браузере
- **Масштабируемость** - готово к росту

---

## 📦 Архитектура

```
Frontend (HTML/CSS/JS)
    ↓
API Client (api-client.js)
    ↓
Vercel Serverless Functions (api/*.js)
    ↓
PostgreSQL Database (Neon.tech)
```

### Файлы проекта:

**Frontend (новые API версии):**
- `js/api-client.js` - клиент для работы с API
- `js/main-api.js` - главная страница
- `js/teams-api.js` - страница команд
- `js/archive-api.js` - архив турниров
- `js/admin-api.js` - админ-панель

**Backend API:**
- `api/tournaments.js` - управление турнирами
- `api/disciplines.js` - управление дисциплинами
- `api/links.js` - ссылки на регистрацию
- `api/social.js` - социальные ссылки
- `api/teams.js` - управление командами

**База данных:**
- `db/schema.sql` - структура БД
- `db/init.js` - скрипт инициализации

---

## 🚀 Быстрый старт

### Вариант 1: Vercel + Neon (Production - рекомендуется)

Следуйте инструкциям в `QUICK_START.md` (5 минут)

### Вариант 2: Локальная разработка

```bash
# 1. Установите зависимости
npm install

# 2. Создайте базу на Neon.tech и получите строку подключения

# 3. Установите переменную окружения
export DATABASE_URL="ваша_строка_подключения"

# 4. Инициализируйте базу
npm run db:init

# 5. Запустите локальный сервер Vercel
npx vercel dev

# 6. Откройте http://localhost:3000
```

---

## 🎯 API Endpoints

### Турниры
- `GET /api/tournaments?status=active` - получить турниры
- `POST /api/tournaments` - создать турнир
- `PUT /api/tournaments` - обновить турнир
- `DELETE /api/tournaments?id=1` - удалить турнир

### Дисциплины
- `GET /api/disciplines` - получить список
- `POST /api/disciplines` - добавить дисциплину
- `DELETE /api/disciplines?name=CS%202` - удалить

### Ссылки на регистрацию
- `GET /api/links` - получить все ссылки
- `POST /api/links` - сохранить ссылки

### Социальные ссылки
- `GET /api/social` - получить ссылки
- `POST /api/social` - сохранить ссылки

### Команды
- `GET /api/teams` - получить все команды
- `GET /api/teams?tournamentId=1` - команды турнира
- `POST /api/teams` - добавить команду
- `PUT /api/teams` - обновить команду
- `DELETE /api/teams?id=1` - удалить команду

---

## 🗄️ База данных

### Таблицы:

**tournaments**
```sql
id, title, discipline, date, prize, teams, max_teams,
custom_link, status, winner, created_at, updated_at
```

**disciplines**
```sql
id, name, created_at
```

**registration_links**
```sql
id, discipline, link, created_at, updated_at
```

**social_links**
```sql
id, platform, link, created_at, updated_at
```

**registered_teams**
```sql
id, tournament_id, name, captain, players,
registration_date, created_at
```

---

## 🔐 Безопасность

### Текущая реализация:
- Простая парольная защита админ-панели
- CORS настроен на `*` (все источники)
- SSL для подключения к БД

### Рекомендации для production:
1. **Измените пароль** в `js/admin-api.js`
2. **Настройте CORS** - разрешите только ваш домен
3. **Добавьте Firebase Auth** для продвинутой авторизации
4. **Rate limiting** - ограничьте количество запросов
5. **Input validation** - проверяйте данные на сервере

---

## 📊 Мониторинг

### Vercel Dashboard:
- **Analytics** - статистика посещений
- **Logs** - логи выполнения функций
- **Deployments** - история деплоев

### Neon Dashboard:
- **Monitoring** - использование БД
- **SQL Editor** - прямой доступ к данным
- **Backups** - автоматические резервные копии

---

## 🔄 Обновление кода

```bash
# 1. Внесите изменения в код
# 2. Закоммитьте в Git
git add .
git commit -m "Update: описание изменений"
git push

# 3. Vercel автоматически задеплоит новую версию!
```

---

## 🆘 Troubleshooting

### Ошибка: "API is not defined"
**Решение:** Убедитесь, что `api-client.js` подключен ПЕРЕД другими скриптами

### Ошибка: "Failed to fetch"
**Решение:** 
1. Проверьте консоль браузера (F12)
2. Убедитесь, что `POSTGRES_URL` настроен в Vercel
3. Проверьте, что API endpoints работают

### Турниры не отображаются
**Решение:**
1. Очистите кэш (Ctrl+Shift+R)
2. Проверьте, что база данных инициализирована
3. Посмотрите логи в Vercel Dashboard

---

## 🚧 Roadmap

- [ ] Firebase Authentication
- [ ] Rate limiting для API
- [ ] Кэширование данных
- [ ] WebSocket для real-time обновлений
- [ ] Загрузка изображений турниров
- [ ] Экспорт данных в Excel
- [ ] Уведомления о новых турнирах
- [ ] Многоязычность (EN/RU)

---

## 📚 Документация

- `DATABASE_SETUP.md` - полная инструкция по настройке БД
- `QUICK_START.md` - быстрый старт за 5 минут
- `VERCEL_DEPLOY.md` - гайд по деплою на Vercel

---

## 💻 Технологии

- **Frontend**: Vanilla JavaScript (ES6+), HTML5, CSS3
- **Backend**: Vercel Serverless Functions (Node.js)
- **Database**: PostgreSQL (Neon.tech)
- **Hosting**: Vercel
- **Version Control**: Git + GitHub

---

## 📄 Лицензия

Проект создан для WB Cyber Club.

---

**Приятного использования! 🎮✨**

Если есть вопросы - смотрите документацию или обращайтесь к разработчику.

