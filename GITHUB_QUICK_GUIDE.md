# 🚀 Быстрая загрузка на GitHub

## Вариант 1: Автоматический (РЕКОМЕНДУЕТСЯ)

### Первая настройка (делается один раз):

**Двойной клик на:** `SETUP-GITHUB.bat`

Скрипт спросит:
1. URL репозитория GitHub
2. Автоматически инициализирует Git
3. Добавит все файлы
4. Сделает первый коммит
5. Отправит на GitHub

### Последующие обновления:

**Двойной клик на:** `PUSH-TO-GITHUB.bat`

Скрипт:
1. Добавит все изменённые файлы
2. Спросит сообщение коммита
3. Отправит на GitHub

---

## Вариант 2: Вручную через PowerShell

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (зелёная кнопка)
3. Назовите: `wb-cyber-club` (или любое другое имя)
4. **НЕ добавляйте** README, .gitignore, license
5. Нажмите **"Create repository"**
6. **Скопируйте URL** (будет показан на следующей странице)

### Шаг 2: Откройте PowerShell в папке проекта

Щёлкните правой кнопкой в папке проекта → **"Open in Terminal"** или **"Открыть в PowerShell"**

### Шаг 3: Выполните команды

```powershell
# 1. Инициализируйте Git
git init

# 2. Переключитесь на ветку main
git branch -M main

# 3. Добавьте все файлы
git add .

# 4. Сделайте коммит
git commit -m "Initial commit: WB Cyber Club with API"

# 5. Добавьте remote (ЗАМЕНИТЕ на ваш URL!)
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git

# 6. Отправьте на GitHub
git push -u origin main
```

---

## 🔐 Авторизация в GitHub

### Если Git просит логин/пароль:

**Windows:**
1. Скачайте [Git Credential Manager](https://github.com/git-ecosystem/git-credential-manager/releases)
2. Установите
3. При следующем push откроется окно авторизации GitHub

**Или используйте Personal Access Token:**

1. GitHub.com → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выберите `repo` права
4. Скопируйте токен
5. При запросе пароля вставьте токен

---

## 📝 Пример первой настройки

```powershell
# В PowerShell в папке проекта:

git init
git branch -M main
git add .
git commit -m "Initial commit: WB Cyber Club"
git remote add origin https://github.com/username/wb-cyber-club.git
git push -u origin main
```

Готово! ✅

---

## 🔄 Обновление кода (после изменений)

### Способ 1: Через bat-файл
Двойной клик на `PUSH-TO-GITHUB.bat`

### Способ 2: Через PowerShell
```powershell
git add .
git commit -m "Описание изменений"
git push
```

---

## 📊 Полезные команды Git

```powershell
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- filename

# Посмотреть remote URL
git remote -v

# Изменить remote URL
git remote set-url origin https://github.com/NEW_URL.git
```

---

## ❌ Распространённые ошибки

### Ошибка: "fatal: not a git repository"
**Решение:** Выполните `git init`

### Ошибка: "fatal: remote origin already exists"
**Решение:** 
```powershell
git remote remove origin
git remote add origin https://github.com/ваш-url.git
```

### Ошибка: "failed to push"
**Причины:**
1. Неправильный URL репозитория
2. Нет прав доступа
3. Нужна авторизация

**Решение:** Проверьте авторизацию в Git

### Ошибка: "RPC failed; HTTP 413"
**Решение:** Файлы слишком большие
```powershell
git config http.postBuffer 524288000
```

---

## 🎯 Что будет загружено на GitHub

✅ Все HTML, CSS, JS файлы
✅ API endpoints (api/*.js)
✅ База данных (schema.sql)
✅ Документация (*.md)
✅ Конфигурация (vercel.json, package.json)

❌ НЕ будет загружено (указано в .gitignore):
- node_modules/
- .env (переменные окружения)
- Временные файлы
- Логи

---

## ✅ Проверка после загрузки

1. Откройте ваш репозиторий на github.com
2. Убедитесь, что все файлы там
3. Должно быть ~50-60 файлов
4. Проверьте, что `.env` НЕТ в списке файлов

---

## 📌 После загрузки на GitHub

**Следующий шаг:** Деплой на Vercel

Смотрите инструкцию: `DEPLOY_TO_VERCEL.md`

---

## 🆘 Нужна помощь?

- [GitHub Docs](https://docs.github.com/)
- [Git Documentation](https://git-scm.com/doc)
- Проверьте `DEPLOY_TO_VERCEL.md` для следующих шагов

**Удачи! 🚀**

