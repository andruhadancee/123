# 🗄️ Настройка базы данных для WB Cyber Club

## Шаг 1: Создание базы данных на Neon.tech

### 1.1 Регистрация и создание проекта

1. Перейдите на [neon.tech](https://neon.tech)
2. Зарегистрируйтесь или войдите через GitHub
3. Нажмите **"Create a project"**
4. Укажите:
   - **Project name**: `wb-cyber-club`
   - **Region**: выберите ближайший регион (например, Europe или Asia)
   - **Postgres version**: оставьте по умолчанию
5. Нажмите **"Create project"**

### 1.2 Получение строки подключения

После создания проекта вы увидите строку подключения:

```
postgres://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require
```

**Сохраните эту строку!** Она понадобится для настройки Vercel.

### 1.3 Инициализация базы данных

#### Вариант A: Через Neon Dashboard (рекомендуется)

1. В Neon Dashboard перейдите в **SQL Editor**
2. Откройте файл `db/schema.sql` из вашего проекта
3. Скопируйте всё содержимое файла
4. Вставьте в SQL Editor
5. Нажмите **"Run"**
6. ✅ База данных готова!

#### Вариант B: Через командную строку

```bash
# Установите зависимости
npm install

# Установите переменную окружения
# Windows PowerShell:
$env:DATABASE_URL="ваша_строка_подключения"

# Windows CMD:
set DATABASE_URL=ваша_строка_подключения

# Linux/Mac:
export DATABASE_URL="ваша_строка_подключения"

# Запустите инициализацию
npm run db:init
```

---

## Шаг 2: Настройка Vercel

### 2.1 Установка Vercel CLI (опционально)

```bash
npm install -g vercel
```

### 2.2 Деплой через Vercel Dashboard (рекомендуется)

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий GitHub
5. В настройках проекта:
   - **Framework Preset**: Other
   - **Build Command**: `npm run build` (или оставьте пустым)
   - **Output Directory**: оставьте пустым
   - **Install Command**: `npm install`

### 2.3 Настройка переменных окружения

После создания проекта:

1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте переменную:
   - **Name**: `POSTGRES_URL`
   - **Value**: ваша строка подключения из Neon
   - **Environment**: выберите все (Production, Preview, Development)
3. Нажмите **"Save"**

### 2.4 Redeploy

После добавления переменных:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки → **"Redeploy"**
4. ✅ Ваш сайт теперь работает с базой данных!

---

## Шаг 3: Проверка работы

### 3.1 Откройте ваш сайт

Перейдите по ссылке вашего Vercel проекта (например, `https://your-project.vercel.app`)

### 3.2 Зайдите в админ-панель

1. Откройте `/admin.html`
2. Введите пароль: `admin123`
3. Попробуйте добавить турнир

### 3.3 Проверьте отображение

1. Вернитесь на главную страницу
2. Убедитесь, что турнир отображается
3. Откройте сайт в другом браузере/устройстве
4. ✅ Данные должны быть одинаковыми везде!

---

## Шаг 4: Миграция существующих данных (если есть)

Если у вас уже есть данные в localStorage, их можно экспортировать:

### 4.1 Экспорт из localStorage

1. Откройте админ-панель в старой версии сайта
2. Откройте консоль браузера (F12)
3. Выполните:

```javascript
// Экспорт турниров
const data = localStorage.getItem('wbcyber_tournaments');
console.log('Турниры:', data);

// Экспорт ссылок
const links = localStorage.getItem('wbcyber_registration_links');
console.log('Ссылки:', links);

// Экспорт социальных ссылок
const social = localStorage.getItem('wbcyber_social_links');
console.log('Социальные:', social);
```

### 4.2 Импорт через админ-панель

Добавьте турниры вручную через новую админ-панель с API.

---

## Структура базы данных

### Таблицы:

1. **tournaments** - активные и прошедшие турниры
   - id, title, discipline, date, prize
   - teams, max_teams, custom_link
   - status ('active' | 'finished'), winner

2. **registration_links** - ссылки на формы регистрации по дисциплинам
   - id, discipline, link

3. **social_links** - социальные ссылки
   - id, platform ('twitch' | 'telegram' | 'contact'), link

4. **disciplines** - список игровых дисциплин
   - id, name

5. **registered_teams** - зарегистрированные команды
   - id, tournament_id, name, captain
   - players, registration_date

---

## Troubleshooting

### Проблема: "Cannot connect to database"

**Решение:**
1. Проверьте правильность строки подключения в Vercel
2. Убедитесь, что переменная называется `POSTGRES_URL`
3. Сделайте Redeploy после добавления переменных

### Проблема: "Table does not exist"

**Решение:**
1. Убедитесь, что вы выполнили `schema.sql` в Neon Dashboard
2. Проверьте, что все таблицы созданы в SQL Editor
3. Попробуйте запустить скрипт снова

### Проблема: Данные не синхронизируются

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+R)
2. Проверьте консоль браузера на ошибки (F12)
3. Убедитесь, что используются файлы `*-api.js`, а не старые

---

## Полезные команды

```bash
# Проверить подключение к базе
node -e "const { Pool } = require('pg'); const pool = new Pool({connectionString: process.env.DATABASE_URL}); pool.query('SELECT NOW()').then(r => console.log('✅ Connected:', r.rows[0])).catch(e => console.error('❌ Error:', e));"

# Локальная разработка с Vercel
vercel dev

# Деплой на Vercel
vercel --prod
```

---

## Дальнейшие улучшения

- [ ] Добавить авторизацию через Firebase Auth
- [ ] Настроить автоматическое резервное копирование Neon
- [ ] Добавить rate limiting для API
- [ ] Настроить мониторинг и логирование

---

**Готово! 🎉** Ваш сайт теперь использует централизованную базу данных!

