# 🚀 Деплой на Vercel - Пошаговая инструкция

## ✅ Предварительная проверка

Убедитесь, что у вас есть:
- ✅ База данных на Neon (инициализирована)
- ✅ Строка подключения:
  ```
  postgresql://neondb_owner:npg_8UamG3Noelwx@ep-lucky-unit-aeydxdjf-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require
  ```
- ✅ Аккаунт на GitHub
- ✅ Аккаунт на Vercel

---

## 📦 Шаг 1: Загрузка на GitHub

### Если репозиторий ещё не создан:

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"New repository"**
3. Назовите репозиторий (например, `wb-cyber-club`)
4. **НЕ** добавляйте README, .gitignore или лицензию
5. Нажмите **"Create repository"**

### В PowerShell в папке проекта:

```powershell
# Инициализируйте Git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit - WB Cyber Club with API"

# Добавьте удалённый репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправьте код
git branch -M main
git push -u origin main
```

---

## 🌐 Шаг 2: Деплой на Vercel

### 1. Импорт проекта

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий из списка
5. Нажмите **"Import"**

### 2. Настройки проекта (оставьте по умолчанию)

- **Framework Preset**: Other (или оставьте автоопределение)
- **Root Directory**: `./` (по умолчанию)
- **Build Command**: оставьте пустым или `npm run build`
- **Output Directory**: оставьте пустым
- **Install Command**: `npm install` (автоматически)

### 3. НЕ нажимайте Deploy пока!

Сначала нужно добавить переменные окружения.

---

## 🔐 Шаг 3: Переменные окружения

### В интерфейсе Vercel (перед деплоем):

1. Разверните **"Environment Variables"**
2. Добавьте переменную:
   ```
   Name:  POSTGRES_URL
   Value: postgresql://neondb_owner:npg_8UamG3Noelwx@ep-lucky-unit-aeydxdjf-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require
   ```
3. Выберите **все окружения**: Production, Preview, Development
4. Нажмите **"Add"**

### Теперь можно деплоить!

5. Нажмите **"Deploy"**
6. Подождите 1-2 минуты ⏳

---

## 🎉 Шаг 4: Проверка

### После успешного деплоя:

1. Vercel даст вам ссылку типа: `https://your-project.vercel.app`
2. Откройте эту ссылку
3. Должна загрузиться главная страница

### Тест функционала:

1. Перейдите на `/admin.html`
2. Введите пароль: `admin123`
3. Попробуйте добавить турнир:
   - Название: "Тестовый турнир"
   - Дисциплина: CS 2
   - Дата: любая
   - Призовой фонд: 10000 ₽
   - Макс команд: 16
4. Нажмите **"Сохранить"**

5. Вернитесь на главную страницу
6. ✅ **Турнир должен отображаться!**

### Проверка синхронизации:

1. Откройте сайт на другом устройстве/браузере
2. ✅ **Тот же турнир должен быть виден!**

---

## 🔄 Обновление кода после изменений

После любых изменений в коде:

```powershell
git add .
git commit -m "Описание изменений"
git push
```

**Vercel автоматически задеплоит новую версию!** 🚀

---

## ⚙️ Дополнительные настройки Vercel

### Настройка домена (опционально):

1. В проекте на Vercel: **Settings** → **Domains**
2. Добавьте ваш домен
3. Настройте DNS записи как показано

### Настройки проекта:

- **Settings** → **General**: название проекта, регион
- **Settings** → **Environment Variables**: управление переменными
- **Settings** → **Git**: настройки интеграции с GitHub

---

## 📊 Мониторинг и логи

### Где смотреть логи:

1. В проекте на Vercel: **Deployments**
2. Выберите деплой
3. Нажмите **"View Function Logs"**
4. Здесь видны все запросы к API и ошибки

### Аналитика:

- **Analytics**: статистика посещений
- **Speed Insights**: производительность

---

## 🐛 Troubleshooting

### Ошибка: "Failed to load resource: 500"

**Причина:** API не может подключиться к базе данных

**Решение:**
1. Проверьте, что `POSTGRES_URL` добавлен в Environment Variables
2. Убедитесь, что строка подключения правильная (со всеми символами)
3. Сделайте **Redeploy**: Deployments → последний деплой → три точки → Redeploy

### Турниры не отображаются

**Решение:**
1. Откройте консоль браузера (F12)
2. Посмотрите на ошибки
3. Проверьте вкладку **Network** - есть ли запросы к `/api/tournaments`
4. Если запросы красные (ошибка) - смотрите логи в Vercel

### API возвращает пустые данные

**Решение:**
1. Убедитесь, что база данных инициализирована (schema.sql выполнен)
2. Проверьте в Neon Dashboard → SQL Editor:
   ```sql
   SELECT * FROM tournaments;
   ```
3. Если таблица пустая - добавьте данные через админку

---

## 🔒 Важно для безопасности!

### ⚠️ ОБЯЗАТЕЛЬНО измените пароль админки:

Откройте файл `js/admin-api.js`, найдите строку:

```javascript
if (password === 'admin123') {
```

Измените на:

```javascript
if (password === 'ВАШ_НАДЁЖНЫЙ_ПАРОЛЬ') {
```

Затем:
```powershell
git add .
git commit -m "Change admin password"
git push
```

---

## 📱 Тестирование на разных устройствах

После деплоя проверьте:
- ✅ Desktop (Chrome, Firefox, Edge)
- ✅ Mobile (Safari на iPhone, Chrome на Android)
- ✅ Tablet
- ✅ В режиме инкогнито

Данные должны быть **одинаковыми везде**! 🌍

---

## ✅ Чек-лист готовности к запуску

- [ ] Код на GitHub
- [ ] Проект создан на Vercel
- [ ] `POSTGRES_URL` добавлен в Environment Variables
- [ ] Первый деплой успешен
- [ ] Главная страница открывается
- [ ] Админка работает (можно добавить турнир)
- [ ] Турнир отображается на главной
- [ ] Данные синхронизируются между устройствами
- [ ] Пароль админки изменён
- [ ] Социальные ссылки настроены
- [ ] Ссылки на регистрацию добавлены

---

## 🎊 Готово!

Ваш сайт WB Cyber Club теперь:
- ✅ Работает на продакшене
- ✅ Использует централизованную базу данных
- ✅ Синхронизирован между всеми пользователями
- ✅ Автоматически обновляется при push в GitHub
- ✅ Имеет быстрый и красивый интерфейс

**Следующие шаги:**
1. Настройте контент (турниры, ссылки, соцсети)
2. Протестируйте на разных устройствах
3. Поделитесь ссылкой с пользователями
4. Мониторьте логи первые дни

**Успехов! 🚀🎮**

