# 📝 История изменений

## [2.0.0] - Обновление функций времени и архивирования

### ✨ Новые возможности

#### Для игроков:
- **Умные кнопки регистрации**: Автоматическое переключение на "Смотреть турнир" в момент старта
- **Кнопка "Смотреть турнир"**: Появляется во время трансляции с указанной ссылкой на стрим
- **Визуальная индикация**: Зелёный фон в календаре для дней с идущими турнирами
- **Автоматическое архивирование**: Турниры исчезают из активных в 23:59 дня проведения

#### Для администраторов:
- **Время начала турнира**: Обязательное поле времени старта по МСК при добавлении турнира
- **Ссылка на трансляцию**: Возможность указать ссылку на Twitch/YouTube стрим
- **Ручной перенос в архив**: Кнопка "Перенести в архив" для активных турниров
- **Добавление через календарь**: Создание турниров из календаря с поддержкой всех новых полей

### 🔧 Технические улучшения

#### База данных:
- Добавлено поле `start_time` в таблицы `tournaments` и `calendar_events`
- Поле `watch_url` уже существовало, теперь используется активнее

#### API:
- Обновлены `api/tournaments.js` и `api/calendar.js` для поддержки `start_time`
- Создан новый эндпоинт `api/archive-auto.js` для автоматического архивирования
- Добавлен Vercel Cron Job на 23:59 каждый день

#### Frontend:
- Удалено автоматическое обновление турниров каждые 30 секунд (вызывало дёргание)
- Добавлено умное обновление только кнопок каждую минуту (без визуальных артефактов)
- Единообразие loader'ов на всех страницах
- Логика определения статуса турнира (до/во время/после) реализована на клиенте

### 📁 Новые файлы

- `db/add-start-time.sql` - миграция для добавления поля start_time
- `api/archive-auto.js` - автоматический перенос турниров в архив
- `CHANGELOG.md` - история изменений

### 🔄 Обновленные файлы

- `admin.html` - добавлены поля start_time и watch_url в формы
- `js/admin-api.js` - обработка новых полей, кнопка архивирования
- `js/main-api.js` - умные кнопки и автообновление
- `js/calendar-page.js` - зелёный фон для идущих турниров
- `vercel.json` - добавлен cron job для автоархивирования
- `README.md` - обновлена документация

### 🚀 Миграция

**✅ АВТОМАТИЧЕСКАЯ!** API автоматически добавит новые поля при первом запуске.

(Опционально: файл `db/add-start-time.sql` существует для ручной миграции)

### 🐛 Исправления

- Устранено дёргание карточек турниров при автообновлении
- Унифицированы loader'ы на всех страницах
- Исправлена работа форм в админ-панели при редактировании
- Упрощена логика кнопок: убрана проверка на 3 часа, кнопка меняется только при старте
- Исправлено: календарные события удаляются при архивировании турниров
- Добавлено автообновление кнопок в модальном окне календаря
- Добавлено отображение времени старта на карточках турниров
- Добавлено отображение времени старта в модальном окне календаря
- Исправлено: таймер теперь учитывает время старта турнира (появление "Турнир начался" в точное время)
- Исправлено: бейдж "Турнир начался" имеет корректную ширину (как другие кнопки)
- Исправлено: парсинг времени теперь поддерживает формат HH:MM:SS из PostgreSQL
- Исправлено: отображение времени старта показывает только HH:MM (без секунд)
- Исправлено: парсинг даты поддерживает форматы YYYY-MM-DD, DD.MM.YYYY и "день месяц год"
- Исправлено: бейдж "Турнир начался" выровнен по размеру кнопки "Подать заявку" (padding, font-size)
- Добавлено логирование в календаре для отладки проверки активности турнира
- Добавлено поле watch_url в таблицу calendar_events для поддержки ссылок на трансляцию
- Исправлены API endpoints календаря для сохранения watch_url при создании и обновлении событий
- Исправлено: при создании события календаря для существующего турнира обновляется watch_url
- Добавлена дефолтная ссылка на Twitch канал: https://www.twitch.tv/wbteamcyberclub (если watch_url не указан)

### 📊 Статистика

- Добавлено: 3 новых API эндпоинта
- Изменено: 12 файлов
- Новых полей БД: 2
- Новых функций: 8

