# 📦 Инструкция по установке на Veroid (Node.js)

## Шаг 1: Создание сервера Node.js

1. Зайдите в панель управления Veroid
2. Перейдите в раздел **"Ядро"** или **"Боты и прочее"**
3. Выберите **[Bot] Node.js**
4. Создайте новый сервер:
   - Выберите нужный тариф
   - Укажите имя сервера (например: "WB Cyber Club")
   - Нажмите "Создать"

## Шаг 2: Загрузка файлов

### Вариант A: Через FTP (рекомендуется)

1. В панели Veroid найдите данные для FTP подключения:
   - Хост (обычно IP адрес)
   - Порт (обычно 21)
   - Логин
   - Пароль

2. Подключитесь к FTP с помощью программы:
   - **FileZilla** (бесплатно): https://filezilla.ru/
   - **WinSCP** (для Windows)
   - Или любой другой FTP-клиент

3. Загрузите ВСЕ файлы проекта в корневую директорию сервера:
   ```
   /
   ├── server.js
   ├── package.json
   ├── index.html
   ├── teams.html
   ├── archive.html
   ├── admin.html
   ├── css/
   │   ├── style.css
   │   └── admin.css
   └── js/
       ├── tournaments-data.js
       ├── main.js
       ├── teams.js
       ├── archive.js
       ├── admin.js
       └── firebase-config.js
   ```

### Вариант B: Через файловый менеджер Veroid

1. В панели управления откройте "Файловый менеджер"
2. Загрузите файлы по одному или архивом (если поддерживается)
3. Убедитесь, что структура папок сохранена

## Шаг 3: Настройка сервера

1. В панели Veroid перейдите в настройки вашего Node.js сервера

2. Укажите параметры запуска:
   - **Главный файл (Main file):** `server.js`
   - **Команда запуска (Start command):** `node server.js`
   - **Порт (Port):** обычно присваивается автоматически

3. Сохраните настройки

## Шаг 4: Запуск

1. Нажмите кнопку **"Запустить"** или **"Start"**
2. Подождите 10-20 секунд пока сервер запустится
3. В логах должно появиться:
   ```
   =================================
   🎮 WB Cyber Club Server запущен!
   🌐 Сервер доступен по адресу: http://localhost:3000
   =================================
   ```

## Шаг 5: Проверка

1. В панели Veroid найдите IP адрес или домен вашего сервера
2. Откройте в браузере: `http://ваш-ip:порт`
   - Например: `http://185.123.45.67:3000`
3. Должна открыться главная страница с турнирами

## Шаг 6: Настройка домена (опционально)

Если хотите использовать свой домен:

1. В настройках Veroid привяжите домен к серверу
2. В DNS вашего домена добавьте A-запись:
   ```
   A-запись: @ → IP сервера
   ```
3. Подождите 1-24 часа для распространения DNS
4. Сайт будет доступен по адресу: `http://ваш-домен.ru`

## 🔧 Возможные проблемы

### Сервер не запускается

**Проблема:** "Cannot find module 'xyz'"
**Решение:** Убедитесь, что файл `server.js` загружен корректно

**Проблема:** "Port already in use"
**Решение:** В панели Veroid измените порт или перезапустите сервер

### Сайт не открывается

**Проблема:** ERR_CONNECTION_REFUSED
**Решение:** 
- Проверьте, что сервер запущен (статус "Running" в панели)
- Проверьте правильность IP адреса и порта
- Убедитесь, что файрвол не блокирует соединение

**Проблема:** 404 Not Found на всех страницах
**Решение:**
- Убедитесь, что все HTML файлы загружены в корень
- Проверьте права доступа к файлам

### CSS/JS не загружаются

**Проблема:** Сайт открывается, но без стилей
**Решение:**
- Проверьте, что папки `css/` и `js/` загружены полностью
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что структура папок правильная

## 📊 Мониторинг

В панели Veroid вы можете:
- Смотреть логи сервера (раздел "Логи" или "Logs")
- Видеть использование ресурсов (CPU, RAM)
- Перезапускать сервер при необходимости

## 🔄 Обновление сайта

Чтобы обновить сайт:

1. Загрузите новые файлы через FTP (перезаписав старые)
2. В панели Veroid нажмите **"Перезапустить"** (Restart)
3. Обновите страницу в браузере (Ctrl+F5)

## 💡 Полезные команды

Если у вас есть SSH доступ к серверу:

```bash
# Просмотр логов
tail -f logs/latest.log

# Проверка, работает ли сервер
ps aux | grep node

# Перезапуск (если есть PM2)
pm2 restart server
```

## ⚙️ Оптимизация

Для production можно использовать PM2 (если поддерживается):

1. Создайте файл `ecosystem.config.js`:
```javascript
module.exports = {
  apps: [{
    name: 'wb-cyber-club',
    script: './server.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
  }]
}
```

2. Запустите через PM2:
```bash
pm2 start ecosystem.config.js
pm2 save
```

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в панели Veroid
2. Откройте консоль браузера (F12) и посмотрите ошибки
3. Убедитесь, что все файлы загружены правильно
4. Обратитесь в поддержку Veroid

---

**Готово! Ваш сайт должен работать! 🎮✨**

